# 남은 작업(TODO)

## P0 (운영 안정성)
- [ ] 실서버 포트/포워딩 최종 점검: `15005/udp`(외부) → 내부 UDP 리스너(`5005` 또는 `15005`) 매핑 확인.
- [ ] 브리지/웹앱 PM2 프로세스 이름과 실행 환경변수 문서화(운영 표준 커맨드 1세트 확정).
- [ ] HTTPS 환경 사용 시 `wss://` 경로 구성 여부 확정(Nginx/리버스 프록시 포함).
- [ ] 실데이터 기준 엔디안/비트순서 설정값 확정(대시보드 기본값 고정 여부 결정).

## P1 (기능/품질 보완)
- [ ] 브리지 헬스체크(예: 최근 UDP 수신 시각) 또는 최소 상태 로그 규칙 추가.
- [ ] 상태 코드 매핑(0/1/2/3 외 코드) 운영 정책 확정 및 문서 반영.
- [ ] 기본 WS 주소를 코드 하드코딩 대신 환경변수 중심으로 운영할지 최종 결정.
- [ ] README와 실제 코드 포트(9091/9090/28080) 정합성 재검토 및 통합 정리.
- [ ] Ray 모드 `ceilingHeight(현재 2.4m)`와 `hFov/vFov(현재 45/45)`를 환경변수 또는 설정 UI로 분리.
- [ ] Ray `topdown` 카메라(상부 사선) 거리/높이 값을 실설치 환경에서 캘리브레이션해 기준값 확정.
- [ ] Ray 높이값(`h(mm)`)의 기준좌표(`y=0` 바닥, 센서 축 방향) 문서를 운영 가이드에 명시.

## P2 (정리/개선)
- [ ] `webapp-buildcheck` 같은 임시 빌드 산출물 관리 기준 정리(보관/삭제 원칙).
- [ ] `doc/develop_plan.md` 완료 항목/잔여 항목 표기 최신화.
- [ ] 운영 체크리스트(배포 후 검증 항목: 연결 상태/마지막 수신/141B 유효성) 간단 템플릿 추가.
- [ ] `codex_start`, `codex_end` 파일의 인코딩/확장자(`.md`) 규칙 통일.
- [ ] `doc/work_instructions.md`의 초기 명세(예: WS 8080, 포인트클라우드 중심 설명)와 현재 구현(WS 9090/웹앱 9091/타워형 3D) 간 불일치 정리.
